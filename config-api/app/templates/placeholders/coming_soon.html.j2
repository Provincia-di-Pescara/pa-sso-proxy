{% extends "base.html.j2" %}
{% block title %}{{ title }} — PA SSO Proxy{% endblock %}
{% block content %}
<div class="d-flex align-items-center mb-3">
  <h2 class="me-3">{{ title }}</h2>
  <span class="badge bg-secondary" style="font-size: 0.85rem;">Prossimamente</span>
</div>
<div class="card border-secondary" style="max-width: 600px;">
  <div class="card-body">
    <p class="card-text text-muted">{{ description }}</p>
    <p class="card-text"><small class="text-muted">Questa funzionalità sarà disponibile in una versione futura del proxy.</small></p>
  </div>
</div>
{% endblock %}
